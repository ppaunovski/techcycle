spring:
  application:
    name: TechCycle
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    driver-class-name: org.postgresql.Driver
    password: techcycle
    username: techcycle
    url: jdbc:postgresql://localhost:5432/techcycle
